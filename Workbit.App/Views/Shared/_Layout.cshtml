<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>@ViewData["Title"] - Workbit HCM</title>
	<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
	<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
	<link rel="stylesheet" href="~/Workbit.App.styles.css" asp-append-version="true" />
	<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

	<!-- Add this in your <head> -->
	<link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">

	<style>
		:root {
			--primary: #088395;
			--secondary: #37B7C3;
			--third: #071952;
			--background: #EBF4F6;
			--text-dark: #212121;
			--success: #4CAF50;
			--warning: #FFC107;
			--error: #F44336;
			--contrast: #FFC107;
		}

		body {
			font-family: 'Nunito', sans-serif;
		}

		/* Make footer stick to bottom */
		html, body {
			height: 100%;
			margin: 0;
			display: flex;
			flex-direction: column;
		}

		body {
			background-color: var(--background);
			color: var(--text-dark);
		}

		main {
			flex: 1; /* Pushes footer down when there's not enough content */
		}

		/* Navbar Styling */
		.navbar {
			background-color: var(--third) !important;
			font-size: 20px;
		}

			.navbar.border-bottom {
				border-bottom: none !important;
			}

		.navbar-logo {
			height: 6vh; /* Adjust to fit your navbar */
			width: auto; /* Keeps aspect ratio */
			max-height: 100%;
			margin: 0.25rem 1rem;
		}

		.navbar .btn.btn-light {
			background-color: white;
			color: var(--primary);
			border: var(--primary);
			transition: background-color 0.3s ease, color 0.3s ease;
			font-size: 20px;
			font-weight: 600;
			border-radius: 10px;
		}

			.navbar .btn.btn-light:hover {
				background-color: var(--secondary); /* Green on hover */
				color: white; /* Text turns white */
			}

		.navbar .btn.btn-outline-light {
			font-size: 20px;
			font-weight: 600;
			border-radius: 10px;
		}

		.navbar-nav .nav-link {
			position: relative;
			color: white !important;
			font-weight: 500;
			padding: 0.5rem 1rem;
			transition: color 0.3s ease, transform 0.3s ease, text-shadow 0.3s ease;
			text-decoration: none; /* Ensure no default underline */
		}

			.navbar-nav .nav-link::after {
				content: '';
				position: absolute;
				bottom: 0;
				left: 0;
				width: 0%;
				height: 2px;
				background-color: var(--secondary);
				transition: width 0.3s ease-in-out;
			}

			.navbar-nav .nav-link:hover {
				color: var(--secondary) !important;
				transform: scale(1.05); /* Slight pop */
				text-shadow: 0 0 8px rgba(55, 183, 195, 0.7); /* Soft teal glow */
				text-decoration: none; /* Disable default underline */
			}

				.navbar-nav .nav-link:hover::after {
					width: 100%; /* Only the animated underline shows */
				}


		.logo-with-outline {
			color: white;
			font-weight: bold;
			-webkit-text-stroke: 1px black; /* Outline for text */
			text-stroke: 1px black;
			/* Outline for the icon using drop-shadow */
			filter: drop-shadow(-1px -1px 0 black) drop-shadow(1px -1px 0 black) drop-shadow(-1px 1px 0 black) drop-shadow(1px 1px 0 black);
		}


		.navbar-brand, .navbar-nav .nav-link {
			color: white !important;
			font-weight: 500;
		}

			.navbar-nav .nav-link:hover {
				text-decoration: underline;
			}

		/* Footer */
		footer {
			background-color: var(--primary);
			color: white;
			padding: 1rem;
			text-align: center;
			position: fixed;
			left: 0;
			bottom: 0;
			width: 100%;
		}

			footer a {
				color: var(--secondary);
			}

				footer a:hover {
					text-decoration: underline;
				}

		/* Typography tweaks for Nunito */
		h1, h2, h3, h5 {
			font-family: 'Nunito', sans-serif;
			font-weight: 700; /* Bold headings */
			letter-spacing: 0.5px;
		}

		/* Specific sizing */
		h1 {
			font-size: 3.5rem; /* Larger main headline */
		}

		h2 {
			font-size: 2.5rem;
			margin-bottom: 2rem;
		}

		h3, h5 {
			font-size: 1.6rem;
		}

		p {
			font-size: 1.2rem;
			font-weight: 500;
			line-height: 1.6;
		}

		/* Button adjustments */
		.btn-custom, .btn {
			font-size: 1.2rem;
			font-weight: 600;
			letter-spacing: 0.5px;
		}

		/* Hero section headline contrast */
		.hero-content h1 {
			font-size: 4rem;
			font-weight: 800;
			text-shadow: 0 3px 8px rgba(0,0,0,0.3); /* Pop more over gradient */
		}

		/* Responsive scaling */
		@@media (max-width: 992px) {
			h1 {
				font-size: 2.8rem;
			}

			h2 {
				font-size: 2rem;
			}

			p {
				font-size: 1.5rem;
			}
		}

	</style>
</head>
<body>
	<header>
		<nav class="navbar navbar-expand-sm navbar-toggleable-sm border-bottom box-shadow">
			<div class="container-fluid">
				<a class="navbar-brand d-flex align-items-center logo-with-outline" asp-area="" asp-controller="Home" asp-action="Index">
					<img src="~/images/Workbit-logo.png" alt="Workbit HCM" class="navbar-logo me-2">
				</a>                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
											aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
					<ul class="navbar-nav flex-grow-1">
						<li class="nav-item">
							<a class="nav-link" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
						</li>

						@if (User.IsEmployee())
						{
						}
						@if (User.IsManager())
						{
						}
						@if (User.IsCeo())
						{
							<li class="nav-item">
								<a class="nav-link" asp-area="" asp-controller="Ceo" asp-action="Dashboard">Dashboard</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" asp-area="" asp-controller="Department" asp-action="AllDepartments">Departments</a>
							</li>
						}
						@if (User.IsAdmin())
						{
							
						}
					</ul>
					<div class="d-flex">

						@if (User.IsUser())
						{
							@if (User.IsCeo())
							{
							<li>ceo</li>	
							}
							<a class="btn btn-light me-2 d-flex align-items-center justify-content-center" asp-area="" asp-controller="User" asp-action="Logout">Logout</a>
						}
						else
						{
							<a class="btn btn-light me-2 d-flex align-items-center justify-content-center" asp-area="" asp-controller="User" asp-action="Login">Login</a>
							<a class="btn btn-outline-light" asp-area="" asp-controller="User" asp-action="Register">Sign Up</a>
						}
					</div>
				</div>
			</div>
		</nav>
	</header>

	<div class="container-fluid p-0">
		<main role="main">
			@RenderBody()
		</main>
	</div>

	<footer>
		<div class="container">
			&copy; 2025 - Workbit - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
		</div>
	</footer>

	<script src="~/lib/jquery/dist/jquery.min.js"></script>
	<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
	<script src="~/js/site.js" asp-append-version="true"></script>
	@await RenderSectionAsync("Scripts", required: false)
</body>
</html>
